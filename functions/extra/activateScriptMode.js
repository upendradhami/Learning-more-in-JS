"use strict";// This script activates script mode for the application.
// It sets the script mode to true and updates the application state accordingly.
//// The script mode allows the application to run in a different mode, which may be useful for debugging or testing purposes.
// // The script mode is activated by setting the scriptMode property to true in the application state.


let canDrives = false;
const passtest = true; 

if (passtest) canDrives = true;
if(canDrives) console.log('can drive car ');


let interface = 'Audio';
let private = 332;
let ifelse = "we cannot used these ";